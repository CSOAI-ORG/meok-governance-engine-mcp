{
  "name": "meok-governance-engine-mcp",
  "description": "AI-powered meok governance engine MCP server for agents. Supports full governance report, which frameworks apply, compliance cost estimator. By MEOK AI Labs.",
  "version": "1.0.0",
  "tools": [
    {
      "name": "full_governance_report",
      "description": "Generate a COMPLETE multi-framework governance report in one call.",
      "parameters": {
        "type": "object",
        "properties": {
          "system_name": {
            "type": "string"
          },
          "system_description": {
            "type": "string"
          },
          "jurisdictions": {
            "type": "string"
          },
          "use_case": {
            "type": "string"
          }
        },
        "required": [
          "system_name",
          "system_description"
        ]
      }
    },
    {
      "name": "which_frameworks_apply",
      "description": "Instantly determine which AI governance frameworks apply to your situation.",
      "parameters": {
        "type": "object",
        "properties": {
          "country": {
            "type": "string"
          },
          "industry": {
            "type": "string"
          },
          "ai_use_case": {
            "type": "string"
          }
        },
        "required": [
          "country"
        ]
      }
    },
    {
      "name": "compliance_cost_estimator",
      "description": "Estimate compliance costs and show how MEOK Governance Engine saves money.",
      "parameters": {
        "type": "object",
        "properties": {
          "systems_count": {
            "type": "integer"
          },
          "risk_level": {
            "type": "string"
          },
          "current_certifications": {
            "type": "string"
          }
        },
        "required": []
      }
    },
    {
      "name": "list_all_tools",
      "description": "List all 62 governance tools available in this engine.",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": []
      }
    },
    {
      "name": "compliance_score_engine",
      "description": "Calculate compliance percentage per framework. Input system description, get scored breakdown.",
      "parameters": {
        "type": "object",
        "properties": {
          "system_description": {
            "type": "string"
          },
          "frameworks": {
            "type": "string"
          }
        },
        "required": [
          "system_description"
        ]
      }
    },
    {
      "name": "get_full_audit_trail",
      "description": "Get timestamped audit trail of all governance checks performed.",
      "parameters": {
        "type": "object",
        "properties": {
          "limit": {
            "type": "integer"
          }
        },
        "required": []
      }
    }
  ]
}